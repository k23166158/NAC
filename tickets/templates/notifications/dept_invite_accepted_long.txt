Hello {{ user.get_full_name|default:user.username }},

{{ actor.get_full_name|default:actor.username }} has accepted your invitation to join the department "{{ target.name }}".

You can manage department members from the department page.

Best regards,
The Support Team

